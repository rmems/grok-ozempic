---
engines:
  complexity:
    exclude_paths:
      - "Cargo.lock"
      - "target/"
  duplication:
    exclude_paths:
      - "Cargo.lock"
      - "src/**/*_test.rs"
      - "src/**/tests.rs"
      - ".kilo/"
      - ".codacy/"
      - "target/"
exclude_paths:
  - "Cargo.lock"
  - "target/"
  - ".kilo/"
  - ".codacy/"
  - "docs/"
  - "scripts/testdata/"
  - "scripts/testdata/**"
  - "scripts/dev_generate_int8_export_fixtures.py"
  - "src/core/weight_pack_read.rs"  # test helper temp_dir flagged by Codacy security — isolated test file
