---
engines:
  complexity:
    exclude_paths:
      - "Cargo.lock"
      - "target/"
  duplication:
    exclude_paths:
      - "Cargo.lock"
      - "src/**/*_test.rs"
      - "src/**/tests.rs"
      - ".kilo/"
      - ".codacy/"
      - "target/"
  security:
    exclude_paths:
      - "src/core/weight_pack_read.rs"  # temp_dir flagged by Codacy security — production GOZ1 v2 reader, keep in complexity/duplication
exclude_paths:
  - "Cargo.lock"
  - "target/"
  - ".kilo/"
  - ".codacy/"
  - "docs/"
  - "scripts/testdata/"
  - "scripts/testdata/**"
  - "scripts/dev_generate_int8_export_fixtures.py"
