# Plan: Address PR #26 bot feedback, fix CI, respond as Kilo agent xAI/Grok Build 0.1

**PR**: https://github.com/rmems/grok-ozempic/pull/26  
**Branch**: `feat/met-108-verify-xai-dissect-alignment` (currently at 00e0108)  
**Goal**: Inspect CI failure + every bot comment/review thread, make the minimal correct changes to address root causes and feedback, push, reply to *every* bot comment citing exactly "Kilo agent xAI/Grok Build 0.1", track everything in beads, update memory MCP with workflow prefs observed/used.

**Strict constraints for execution (plan mode rules)**: Only the plan file may be written/edited until `plan_exit`. All investigation via read/grep/glob/webfetch/github_*/bash(readonly)/memory_*/beads_* (readonly or context). No `git commit`, no edits to source, no `cargo` that mutates, no PR comments during planning. Execution phase (after plan_exit + user proceeds) will perform mutations.

**Key facts discovered (plan phase)**:
- CI job "fmt / clippy / test" fails (exit 101 on Clippy step) because `.github/workflows/rust.yml` uses `dtolnay/rust-toolchain@stable` **without** `components: rustfmt, clippy`. `cargo fmt`/`cargo clippy` are unavailable on the runner (matches local "no such command"). Tests themselves pass (87/87 locally via `cargo test --all-targets --all-features`). Also a deprecation warning for `actions/checkout@v4` (Node 20).
- Copilot reviewer job failed with "encountered an error".
- All actionable bot feedback is in 9 review threads (Gemini 3, Macroscope 1, CodeAnt 1, Codex 4) + top-level bot comments (linear-code[bot], codeant-ai[bot] x2). No human review comments.
- Memory MCP (read_graph + searches): No *grok-ozempic-specific* PR workflow entity yet. Cross-project observations (Dioscuri-Cloud, xai-dissect, corinth-canal) show consistent pattern: after bot reviews, make targeted fixes, reply in threads (sometimes as agent e.g. "Kilo agent: OpenAI/GPT-5.5"), push, track externally (github/linear), avoid exposing secrets. User explicitly wants citation "Kilo agent xAI/Grok Build 0.1" for this task.
- Beads: No DB yet for this workspace (`bd stats` / `bd list` fail; `bd where` shows repo but no active). `bd init --prefix goz` will create local Dolt DB `goz`, `.beads/`, auto-commit setup files (AGENTS.md, CLAUDE.md, hooks), install integrations. Follow beads.md rules: track *all* work in bd (no markdown TODOs), use `bd create/claim/update/close`, `bd dolt push` at session end. Prefix will yield `goz-<hash>` IDs.
- Code issues to fix (directly from review threads + root-cause analysis):
  1. **TensorClass equality fragility** (Gemini+Macroscope+CodeAnt+Codex P2): `src/core/alignment.rs:122` does `if expected == &actual`. `TensorClass` derives PartialEq including metadata (`reason`, `rank`, `gif_threshold`). Metadata-only manifest change (e.g. reason text) falsely reports mismatch even if tier (Preserve/fp16/ternary) is identical. Fix: compare only discriminant for the *match decision*. Keep full structs in `mismatches`/`boundary_summary` for diagnostics.
  2. **glob_match allocations** (Gemini): `src/core/selection.rs:128` does two `.split('.').collect::<Vec>()` on every call (called 770+ times in alignment + classify). Fix: iterator-based loop (exact suggestion in review) using existing `segment_match`.
  3. **BTreeMap entry allocs** (Gemini): `src/core/alignment.rs:179` `by_class.entry(label.to_string()).or_insert(0) +=1` allocates String every iteration. Fix: `get_mut` then insert (exact suggestion).
  4. **Embedding must be ternary** (Codex P1 + docs contract): `dissect/grok-1/structural-manifest.json:27` + `src/core/grok1_inventory.rs:29` put `embedding.slot_00.token_embedding` in fp16. But `docs/first-quantization-target.md:55` + artifact contract + `src/artifact.rs` (candidate_saaq_embedding) + `src/core/precision.rs:39` require it to flow through TernarySnN path. `decide()` only maps TernaryCandidate → TernarySnN. Fix: move to `ternary_candidates`, change InventoryTensor to `TernaryCandidate { rank: None, gif_threshold: None }`. Update all 4-5 count/len asserts in `grok1_inventory.rs` tests + `alignment.rs` tests (fp16→0, ternary→449, lens 0/8). This also makes alignment report match "first target" intent.
  5. **V2 manifests must be rejected for runtime until name translation** (Codex P1): Parser was relaxed to accept `MANIFEST_NAME_CONVENTION_V2` so embedded structural manifest parses. But `build_manifest_npy`/`build_manifest_safetensors` + `npy_stem_to_tensor_name` (and safetensors path) always produce V1 names (`blk.0...`); V2 globs (`block_*.slot_*`) would never match → routers/norms/embedding silently get default=ternary. Structural manifest is for alignment verification only. Fix: keep V2 acceptance in `parse_manifest_bytes` (and test) for embedded/alignment use + future; add guard in `src/core/stream.rs:resolve_manifest` (after any `load_manifest`) that if convention==V2, hard error with message referencing the translation gap and suggesting V1 or embedded baseline. (Structural JSON is never loaded via resolve path.)
  6. **i8 tensors "unsupported for streaming"** (Codex P1): The 7 ternary_candidate globs for `*_i8.*` + moe experts cover the 448 i8 tensors from xai-dissect inventory (required for 770/0-unclassified/alignment pass + `classify_full_inventory` test). Raw checkpoint streaming intentionally does `if dtype == SourceDtype::Other { continue; }` (`parse_safetensors_dtype` / `npy_dtype_to_source` only F32/F16/BF16; see stream.rs:518,403,491). i8 surface is incorporated later via wrap_existing_int8_* in artifact.rs + reports/. Do **not** remove rules (would regress coverage goal of this PR). Fix: add precise explanatory comments in the JSON (above the i8 rules), in `grok1_inventory.rs` (near the i8 InventoryTensor pushes), and in `alignment.rs` (in `concrete_coverage_has_no_unclassified_tensors` or `structural_manifest_loads` test). Explain separation of concerns.

- Other bot comments (non-actionable or auto): linear-code linkback, codeant "reviewing..."/"finished", copilot error, macroscope/codeant/codeant-ai neutral/success reviews. Must still reply to all (general "addressed" note + citation) + re-request copilot if possible.
- Memory: Searches found no grok-ozempic PR entity; will create one on exec start + add observations. Cite agent in all replies.
- Verification (always): after edits `cargo test --all-targets --all-features` (must 87+ pass), `cargo check --all-targets --all-features`, `git diff --check`. For fmt/clippy in exec env: `rustup component add rustfmt clippy && cargo fmt --all -- --check && cargo clippy --all-targets --all-features -- -D warnings`. After push, CI must be green (re-run the job if needed via gh).
- Beads workflow (mandatory per rules + top-level instructions): init, create tracking issue, claim, update status as work progresses (never markdown lists), close on done, `bd dolt push` at very end.
- GitHub interaction: Use `github_*` tools (preferred for structured) or `bash` `gh` (for convenience, e.g. `gh pr comment`). For inline replies use `github_add_reply_to_pull_request_comment` (commentId from discussion_rNNNN or `gh api .../pulls/26/comments`). For top-level use `github_add_issue_comment`. Re-request copilot via `github_request_copilot_review`. After fixes+push, comment with summary + citation. Do **not** merge.
- Non-trivial changes: At end of impl (after all edits+verify+push+comments), call `suggest` with `/local-review-uncommitted` (since uncommitted until final commit? follow "Prefer /local-review-uncommitted when work not yet committed").
- Final: `bd dolt push`, memory updates recording what was learned/done, plan_exit not needed in exec.

**Execution order (do not deviate; one logical change per commit where possible)**:

1. **Startup & tracking setup (read-only first)**:
   - `memory_search_nodes` (query variants for "github pr workflow" / "respond to pr bot" / "Kilo agent" / "grok-ozempic").
   - `memory_read_graph`.
   - `memory_create_entities` for new "grok-ozempic-github-pr-workflow" (entityType: "preference") + observations (list exact user requirements from query + cross-project patterns + "Kilo must cite xAI/Grok Build 0.1 on every reply").
   - `beads_context` (set workspace_root, init with prefix "goz").
   - `bash`: `bd init --prefix goz` (accept auto-commit of .beads/ + generated AGENTS.md/CLAUDE.md/hook files; they become part of this PR or a follow-up chore).
   - `bd create "Address CI failure + all bot review feedback on PR #26 (alignment vs xai-dissect)" -t task -p 1 --assignee raulmc` (capture the returned ID e.g. `goz-abc123`; use as $ISSUE).
   - `bd claim $ISSUE`.
   - `bd update $ISSUE --notes "Root cause: missing rustfmt/clippy components in workflow. 9 actionable review threads on TensorClass==, allocs, embedding bucket, V2 runtime, i8 streaming scope."`
   - Checkout/confirm branch: `git checkout feat/met-108-verify-xai-dissect-alignment && git pull --ff-only origin feat/met-108-verify-xai-dissect-alignment` (or rebase if needed; stay on branch).

2. **Inspect why CI failed (confirm plan phase findings)**:
   - `read .github/workflows/rust.yml`.
   - `webfetch` the job URL + run URL (https://github.com/rmems/grok-ozempic/actions/runs/26551316353/job/78213833883) + any log artifacts if public.
   - `github_pull_request_read` (method get_check_runs, get_status) for #26.
   - `bash`: `cargo test --all-targets --all-features` (confirm passes); attempt `cargo fmt --all -- --check` and `cargo clippy...` (expect "no such command" to repro).
   - `bash`: `rustup component list --installed` etc. to confirm components missing locally too.
   - Record observation in beads: `bd update $ISSUE --notes "Confirmed: workflow lacks components → fmt/clippy unavailable → exit 101. Tests pass. Also Node20 deprecation on checkout."`

3. **Read all current bot comments/reviews (full context)**:
   - `github_pull_request_read` (methods: get, get_comments, get_reviews, get_review_comments, get_files, get_diff, get_check_runs) — multiple calls.
   - `webfetch` the PR page + specific discussion_r URLs if needed for full text.
   - `bash`: `gh pr view 26 --json reviews,comments,reviewThreads` (capture IDs, bodies, paths, line numbers).
   - List in notes: the 9 review threads + 3 top-level + copilot error + auto reviews.

4. **Fix CI (first commit)**:
   - `edit .github/workflows/rust.yml`:
     - In "Install Rust" step, add `with: { components: rustfmt, clippy }`.
     - Bump `actions/checkout@v4` → `actions/checkout@v5` (addresses deprecation warning; v5 is compatible).
     - (Optional but recommended) Add comment: "# Components required for fmt/clippy steps; without them cargo subcommands are unavailable and job fails with 101."
   - `bash`: `git add .github/workflows/rust.yml && git commit -m "ci: add rustfmt+clippy components to dtolnay toolchain; bump checkout to v5 (fixes #26 CI failure)

   The 'fmt / clippy / test' job failed because the runner had no rustfmt/clippy (cargo subcommand not found, exit 101 on Clippy step). Matches local repro. Tests themselves were passing.

   Also silences Node 20 deprecation for checkout.

   Kilo agent xAI/Grok Build 0.1"`
   - `bash`: `git push origin feat/met-108-verify-xai-dissect-alignment`
   - Verify: `bash` re-run local with components added (`rustup component add ... && cargo fmt --check && cargo clippy... -- -D warnings`).
   - `bd update $ISSUE --notes "CI workflow fixed and pushed. Will be green after re-run."`

5. **Fix code review feedback (group into 2-3 commits; keep minimal)**:
   - **Commit A: robustness + perf (addresses Gemini 1+2+3, Macroscope, CodeAnt, Codex P2)**:
     - `edit src/core/alignment.rs`:
       - Line ~122: `if expected == &actual {` → `if std::mem::discriminant(expected) == std::mem::discriminant(&actual) {`
       - Add `use std::mem;` at top if needed (or qualify).
       - Line ~179: replace the entry line with the get_mut + insert block (exact from review).
       - (The mismatches vec still stores full TensorClass for human debugging — unchanged.)
     - `edit src/core/selection.rs`:
       - Replace `glob_match` body with the iterator loop version from Gemini review (keep `segment_match` helper + all existing + new prefix tests).
       - Update doc comment if wording needs tiny tweak for iterators.
     - `bash` verify: `cargo test --all-targets --all-features` (alignment + glob + coverage tests must still pass; the discriminant change is observable only on metadata-mismatch cases which our manifests don't trigger).
     - Commit msg: "fix(alignment,selection): use discriminant for tier match; iterator glob_match; get_mut for coverage counts (addresses gemini/macroscope/codeant/codex reviews)

     - TensorClass == was comparing metadata (reason/rank/gif) → false mismatches on non-boundary changes.
     - glob_match + classify_full_inventory were allocating on hot paths (770 tensors).
     - All tests continue to pass; alignment remains strict on *tiers*.

     Kilo agent xAI/Grok Build 0.1"
     - `git add` the two files, commit, push.

   - **Commit B: embedding to ternary + V2 runtime guard (addresses Codex P1s 6+7+9)**:
     - `edit dissect/grok-1/structural-manifest.json`:
       - Move the embedding entry from "fp16" array to "ternary_candidates" array (as first entry or logical place).
       - Update its "reason" if desired to reference the doc.
     - `edit src/core/grok1_inventory.rs`:
       - Change the first InventoryTensor (embedding) `expected_class` from Fp16 { reason: Some("SAAQ candidate...") } to `TernaryCandidate { rank: None, gif_threshold: None }`.
       - Update the comment above it.
       - Update the two class-count asserts (fp16→0, ternary→449 with updated message).
     - `edit src/core/alignment.rs`:
       - Update the 3 asserts in `concrete_coverage_has_no_unclassified_tensors` (fp16 0, ternary 449).
       - Update `structural_manifest_loads` (fp16.len()==0, ternary_candidates.len()==8).
       - (The "preserve_not_ternary" test stays valid.)
     - `edit src/core/stream.rs`:
       - In `resolve_manifest` (after each successful `load_manifest` for path/env cases), add:
         ```rust
         if m.model.tensor_name_convention == crate::core::manifest::MANIFEST_NAME_CONVENTION_V2 {
             return Err(GrokOzempicError::ManifestNameConventionMismatch {
                 got: m.model.tensor_name_convention.clone(),
                 expected: "V1 (block_NNN.slot_SS.kind / V2 naming is for alignment verification only until checkpoint→structural name translation is implemented)".to_string(),
             });
         }
         ```
       - Make sure the error type + message is clear and references the Codex concern + "see src/core/npy.rs + stream.rs build_manifest_* + future translation layer".
     - Also relax? No: the parser change from PR stays (V2 accepted at parse time).
     - Update any nearby comments/tests that hardcode the old "1 fp16" if present in stream tests (unlikely; they use baseline V1).
     - `bash` verify full test suite + check.
     - Commit: "fix(manifest,inventory,alignment,stream): embedding as ternary candidate per docs/contract; reject V2 manifests at runtime resolve (addresses codex P1s)

     - embedding.slot_00 must go through TernarySnN (first-quant-target.md:55, precision::decide, artifact candidate_saaq_embedding). Was incorrectly fp16.
     - V2 structural names now hard-rejected in resolve_manifest (parser still accepts for the embedded alignment path and future). Raw ckpt names remain V1 until translation added.
     - Counts/lens/tests updated; alignment still 770/0-mismatch.

     Kilo agent xAI/Grok Build 0.1"
     - push.

   - **Commit C: i8 scope clarification (addresses Codex P1 #8; no behavior change)**:
     - `edit dissect/grok-1/structural-manifest.json`: add a comment (JSON allows /* */? No — use top-level or a "notes" field? Use preceding line in the array or add a sibling "notes" at top level for the i8 rules. Simplest: add after "ternary_candidates" intro a JSON comment isn't valid, so add a field "_i8_streaming_note": "These 7 patterns cover the 448 i8 tensors from xai-dissect inventory for alignment verification only. See grok1_inventory.rs and alignment.rs. Current stream.rs filters SourceDtype::Other (raw ckpt has no i8); int8 are wrapped in src/artifact.rs wrap_existing_int8_* paths. Do not remove — would break 770-tensor coverage and classify_full_inventory test." (or put the note in "produced_by" or just document in code).
     - Better: since JSON, put the explanation in code comments near the places that matter, and a minimal note in JSON if possible (JSON5 not supported; parser is serde_json). Put the long explanation only in .rs files; in JSON just keep the globs (they are the source of truth for the review concern).
     - `edit src/core/grok1_inventory.rs`: above the i8 pushes (the 7 attn + 3 moe blocks), add a block comment:
       ```rust
       // NOTE (addressing Codex review on PR #26): The 448 i8 tensors (192 moe_expert + 256 attn_proj_i8)
       // are marked TernaryCandidate here to match the xai-dissect structural inventory exactly.
       // Alignment + classify_full_inventory require 0 defaults and exact 448 ternary for 770 coverage.
       // The *streaming* path (build_manifest_*) skips SourceDtype::Other (see parse_safetensors_dtype + npy).
       // i8 data enters via xai-dissect "exports" + wrapping in src/artifact.rs (wrap_existing_int8_expert etc.)
       // + report validation. This manifest declares *logical classification*, not "all these will be
       // float-streamed from raw ckpt". Removing the patterns would regress the purpose of this PR.
       ```
     - Similarly add short comment in `alignment.rs` inside the coverage test or `classify_full_inventory`.
     - `edit src/core/stream.rs` near the Other skip (around 366/403/491/518): add a one-line cross-ref comment: "// i8/Other tensors from xai-dissect inventory are covered by structural manifest for alignment (see grok1_inventory.rs:NOTE and structural-manifest.json); they arrive via artifact wrapping, not this float-only stream path."
     - No test changes needed (behavior identical).
     - Commit msg similar, ending with citation + "no behavior change; documentation only to close the review thread."
     - push.

6. **Local final verification (on the branch after all 3 code commits)**:
   - `rustup component add rustfmt clippy` (if missing).
   - `cargo fmt --all -- --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-targets --all-features && cargo check --all-targets --all-features && git diff --check`.
   - `bash`: `git log --oneline -4` (the 4 commits: CI + A + B + C).
   - `bd update $ISSUE --notes "All fixes implemented + verified locally. 4 commits on branch. Ready to push+comment."`

7. **Push + re-trigger CI**:
   - `git push origin feat/met-108-verify-xai-dissect-alignment` (all commits).
   - Optionally: `gh workflow run rust.yml --ref feat/met-108-verify-xai-dissect-alignment` or just let the push trigger PR CI.
   - `bd update $ISSUE --notes "Pushed. Awaiting green CI on the 4 commits."`

8. **Respond to *every* PR bot comment (use github tools + citation)**:
   - Re-fetch current state: `github_pull_request_read` (get_review_comments etc.) + `bash gh api` to get fresh comment IDs / thread node IDs if needed for resolve.
   - For each of the 9 review threads (use `github_add_reply_to_pull_request_comment` with the discussion_r number as commentId, or the numeric id from API):
     - Reply body must:
       - Reference the exact suggestion/path/line.
       - State what was changed + commit SHA (after push).
       - Explain why (for design ones).
       - End exactly with: "Kilo agent xAI/Grok Build 0.1"
     - Specifics:
       - 3x Gemini threads + Macroscope + CodeAnt discriminant one: "Changed to std::mem::discriminant in alignment.rs:122 (and get_mut + iterator glob in selection). Full structs retained for diagnostics. Tests pass. See commit <shortsha>. Kilo agent xAI/Grok Build 0.1"
       - Codex V2: "Added runtime rejection guard in resolve_manifest for V2 (parser still accepts for embedded structural + alignment). Name translation pending in npy/stream. Kilo..."
       - Codex embedding: "Moved embedding to ternary_candidates in structural-manifest.json + inventory expected_class. Updated counts/tests. Matches first-quantization-target.md contract and precision::decide. Kilo..."
       - Codex i8: "Added explanatory comments in structural-manifest.json (note), grok1_inventory.rs (big NOTE block), alignment.rs, stream.rs. Rules kept (required for 770/alignment). Clarified raw-stream vs artifact-wrap separation. Kilo..."
   - For top-level bot comments (linear, codeant start/finish): `github_add_issue_comment` (issue_number=26) with summary: "Addressed CI root cause (missing components) + all 9 review threads from Gemini/Macroscope/CodeAnt/Codex. Pushed 4 commits. CI should now pass. Kilo agent xAI/Grok Build 0.1" (can be one or threaded replies).
   - For the failed copilot review (the review object): `github_add_issue_comment` noting the error + "Re-requesting review now that fixes are in." Then call `github_request_copilot_review` (owner=rmems, repo=grok-ozempic, pullNumber=26).
   - For any codeant "retrigger" hints: if desired, post "@codeant-ai: review" via comment.
   - After each reply tool call, `bd update $ISSUE --notes "Replied to thread <id or path:line> as Kilo agent..."`.
   - Use `github_pull_request_read` (get_comments etc.) after to confirm posted.

9. **Post-push CI validation + any re-fix**:
   - Poll or wait: `github_pull_request_read` (get_status) or `bash gh run list --branch feat/...`.
   - If any new failure appears (unlikely), inspect logs via webfetch/gh, fix with another commit+push+reply cycle, update beads.
   - Once green: `bd update $ISSUE --status in_progress --notes "CI green on all checks."`

10. **Wrap-up (non-trivial change → suggest + beads close + memory + push)**:
    - `suggest` (after final response text): short summary of changes, actions=[{label:"Local review of uncommitted", prompt:"/local-review-uncommitted"} ] (or committed if we committed the replies? follow guideline: prefer uncommitted if not yet committed the comment metadata; but comments are remote. Use uncommitted for the code diffs).
    - `bd close $ISSUE --reason "Implemented fixes for CI + all 9 bot review threads; responded with citations; pushed; CI green."`
    - `bd dolt push` (sync remote).
    - `memory_add_observations` on the new entity + perhaps the project: "Successfully executed full PR bot response workflow on grok-ozempic#26 as Kilo xAI/Grok 0.1: beads-tracked, addressed discriminant/allocs/embedding/V2/i8/CI, replied to every thread, pushed."
    - `memory_read_graph` (to snapshot).
    - Final `git status`, `git log -3`.
    - Do **not** ask further questions at end; end with final state.

**Tradeoffs resolved (no user question needed)**:
- Kept V2 in parser (required for structural embedded + alignment tests) + rejected only at runtime resolve (minimal, satisfies Codex without regressing PR feature).
- Kept all i8 globs + added docs (coverage is the point of PR; removing would make tests fail and regress "full 770" claim).
- Moved embedding (correct per docs/contract; was a latent bug in the PR).
- 3 small commits for code + 1 for CI (clear history for reviewers) + replies after push.
- Bumped checkout (cheap win, no risk).
- Used beads init (mandatory per project rules; auto-creates files that will be tracked).

**Risks & mitigations**:
- Beads init mutates + auto-commits: expected, document in replies if it touches the PR diff.
- If gh/github_* tools lack write perms in the session: fall back to `bash gh ...` (assume user has gh auth with repo write).
- If after replies CI still red for unrelated reason: open a follow-up issue via beads + comment "unrelated; tracked as goz-xxx".
- No secrets touched (none in this PR).

**Files that will be edited (execution only)**:
- .github/workflows/rust.yml (CI)
- src/core/alignment.rs (discriminant + get_mut)
- src/core/selection.rs (glob iterator)
- dissect/grok-1/structural-manifest.json (embedding move + note)
- src/core/grok1_inventory.rs (embedding class + counts)
- src/core/stream.rs (V2 guard + i8 note)
- (Possibly minor comment-only in other places if needed for clarity)

This plan is complete, self-contained, and ready for direct execution after `plan_exit`. All commands, edit targets, and reply templates are specified.

**End of plan**
