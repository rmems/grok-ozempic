/target

# Beads / Dolt
# issues.jsonl IS tracked on purpose: agents read it to recover project context
# with no network access and no API tokens. GitHub + Linear stay canonical for
# status and handoff; the JSONL is a committed cache of that state.
# interactions.jsonl is NOT tracked -- a short log with little context value, so
# committing it is pure per-commit churn. Everything else here is machine-local.
.dolt/
.beads-credential-key

# Agent / Editor directories
.kilo/
.vscode/
.idea/
.opencode/

# Unreferenced duplicate of .claude/skills/ (GH #96). It is not wired to any
# tool in this repo, nothing tracked references it, and its copy had already
# drifted into a dangling `.Codex/rules/manifests.md` link that exists in no
# casing. `.claude/` is the single source for skills and rules; other agent
# surfaces (.codex/, .cursor/, .devin/) should *reference* it, not fork it.
.agents/

# Generated docs and dependencies (ignore for Codacy analysis)
target/
node_modules/
*.sarif
.codacy/
__pycache__/

# Local Qodana CLI results / HTML report (just qodana; see REVIEW.md)
.qodana/

# Beads machine-local runtime state (issues.jsonl is tracked; see above).
# Only what beads' own tracked .beads/.gitignore does NOT already cover -- it
# handles *.db, *.lock, export-state.json, last-touched, backup/, embeddeddolt/.
# Duplicating those here would create a second source of truth that drifts when
# beads regenerates its file.
.beads/interactions.jsonl
.beads/last_pull
.beads/events.jsonl
.tmp-head
.tmp-pr-babysit/

# Generated INT4 research side-tables (multi-block experiment)
**/int4-side/

# Persistent per-output advisory locks for the GH #85 supervisor.  These must
# not be unlinked after use: existing waiters may still reference their inode.
**/.*.supervisor.lock
